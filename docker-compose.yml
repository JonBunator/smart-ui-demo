services:
  smart-ui-demo:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - src/.env
    ports:
      - "3001:3000"
    restart: unless-stopped
  database:
    image: postgres:latest
    env_file:
      - src/.env
    ports:
      - "5432:5432"
    restart: unless-stopped
    